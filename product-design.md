# 密态沙箱系统（CDS）· 产品设计文档

> 版本：v1.0  
> 作者：@架构师  
> 日期：2026-06-03  
> 对齐标准：TC609-6-2025-01、GM/T 国密系列、NDI-TR-2025-02、《可信数据空间发展行动计划（2024—2028年）》
> 参考项目：Eclipse EDC、GAIA-X、AWS Clean Rooms、Gramine、IDS DSP
> 参考规范：中国信通院《可信数据空间总体框架》、《可信数据空间技术要求》、《数据安全流通技术规范》、《隐私计算技术规范》、《数据沙箱技术规范》

---

## 目录

1. [产品概述](#1-产品概述)
2. [核心业务场景](#2-核心业务场景)
3. [用户角色与旅程](#3-用户角色与旅程)
4. [系统架构设计](#4-系统架构设计)
5. [功能模块详细设计](#5-功能模块详细设计)
6. [安全架构设计](#6-安全架构设计)
7. [业务闭环设计](#7-业务闭环设计)
8. [部署架构](#8-部署架构)
9. [实现计划](#9-实现计划)
10. [风险与应对](#10-风险与应对)

---

## 1. 产品概述

### 1.1 产品定位

密态沙箱系统（Confidential Data Sandbox，CDS）是面向可信数据空间互联互通场景的**数据安全流通基础设施**。核心价值主张：

> **"数据进、算法进、结果出——原始数据不出域，价值安全流通"**

### 1.2 三大安全承诺

| 承诺 | 含义 | 技术保障 | 参考实现 |
|------|------|----------|----------|
| **可用不可见** | 数据在密态环境中被使用，任何人（含运维）无法直接访问原文 | TEE 硬件隔离 + 内存加密 | Gramine SGX、Occlum LibOS |
| **可出不可取** | 仅符合合约约定的计算结果可离开沙箱，原始数据无法导出 | 输出控制网关 + 差分隐私 | AWS Clean Rooms DP、Microsoft Presidio |
| **可审不可改** | 所有操作行为均被细粒度记录并链上存证，不可篡改 | 区块链存证 + 远程证明 | FISCO BCOS、Eclipse EDC 审计 |

### 1.3 核心设计原则

基于开源项目调研提炼的设计原则：

| 原则 | 来源 | 在 CDS 中的应用 |
|------|------|-----------------|
| **控制平面/数据平面分离** | Eclipse EDC / Tractus-X | Contract Engine（控制）↔ Sandbox Runtime（执行）解耦 |
| **数字清算所合规** | GAIA-X Digital Clearing Houses | 输出审查网关 + 链上存证 → 合规证明 |
| **数据清洁室模式** | AWS Clean Rooms | 多方参与、不共享原始数据、差分隐私预算控制 |
| **TEE Library OS** | Gramine / Occlum | 未修改应用直接在 SGX 中运行，Manifest 配置 |
| **连接器协议** | IDS DSP (Data Space Protocol) | 标准化的跨空间连接器间通信协议 |
| **策略即代码** | OPA (Rego) | 合约条款 → 可执行策略包，TEE 内实时评估 |

### 1.4 目标用户

| 角色 | 描述 | 核心诉求 | 对应模块 |
|------|------|----------|----------|
| **数据提供商**（数商） | 企业、政府部门，持有数据资产 | 数据不离域、使用可控、收益可追溯 | F1 数据产品管理 |
| **数据开发商** | 算法/模型开发者，提供数据处理能力 | 算法可自主运行、结果可信、IP 保护 | F2 沙箱运行环境 |
| **数据使用方**（买方） | 金融、医疗、政务企业，需跨方数据分析 | 结果真实可信、成本可控、合规 | F2 + F5 输出审查 |
| **运营方** | 数据空间平台建设者 | 合规、可审计、支持互联互通 | F3 + F6 + F9 |
| **安全运维方** | 安全团队、监管机构 | 全程可追溯、零信任运维、应急响应 | F6 + F7 + F9 |

### 1.5 合规标准

| 标准 | 要求 | CDS 对应能力 |
|------|------|-------------|
| 《数据安全法》(2021) | 数据分级分类管理，重要数据密态处理 | 三级安全体系 L1/L2/L3 |
| 《个人信息保护法》(2021) | 个人信息 TEE 内处理，结果差分隐私保护 | 输出审查网关 DP 注入 |
| TC609-6-2025-01 | 可信数据空间技术架构 | 互联互通接口对齐 |
| GB/T 44249-2024 | 可信数据空间技术架构（国家标准） | 分层架构对齐：基础设施层、数据管控层、服务层 |
| 《可信数据空间发展行动计划（2024—2028年）》 | 2028年建成100+可信数据空间 | 数据沙箱作为核心基础设施 |
| NDI-TR-2025-02 | 数据基础设施互联互通 | 跨空间连接器协议 |
| GM/T 密码标准 | SM2/SM3/SM4 算法强制使用 | 全链路国密 |
| GB/T 22239-2019 | 等保三级 | 安全审计 + 访问控制 |
| 中国信通院《数据沙箱技术规范》 | 沙箱隔离、安全审计、输出管控 | L1/L2/L3 三级沙箱 + 输出审查网关 |
| 中国信通院《隐私计算技术规范》 | 联邦学习、安全多方计算、差分隐私 | MPC + FL + DP 技术栈 |

---

## 2. 核心业务场景

### 2.1 场景一：公共数据授权运营

**背景**：政府公共数据（如企业纳税、社保、不动产）需要安全地向社会开放，支撑金融风控、商业分析等场景。

**业务流程**：

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    公共数据授权运营全流程                                   │
│                                                                         │
│  ① 数据主管部门（数商）                                                    │
│     ├─ 数据分级分类（公开/受限/敏感/核心）                                   │
│     ├─ 数据产品封装（SM4-GCM 加密 + 元数据注册）                            │
│     ├─ 使用策略配置（允许算法/禁止操作/输出限制）                             │
│     └─ 发布至公共数据目录                                                  │
│                                                                         │
│  ② 运营方（数据运营平台）                                                  │
│     ├─ 审核数据产品合规性                                                  │
│     ├─ 审核买方资质（企业征信/行业许可）                                     │
│     ├─ 签发数字合约（使用期限/操作范围/DP 参数）                             │
│     └─ 分配沙箱资源（L1/L2 按数据敏感级别）                                 │
│                                                                         │
│  ③ 数据使用方（买方）                                                     │
│     ├─ 检索公共数据目录                                                   │
│     ├─ 签订数字合约（SM2 双方签名）                                        │
│     ├─ 创建沙箱会话（TEE 远程证明 → 密钥分发）                              │
│     ├─ 上传算法/SQL（代码签名 + 静态扫描）                                  │
│     ├─ 沙箱内执行（TEE 隔离计算）                                          │
│     └─ 获取审查后结果（DP 噪声 + 水印 + 签名）                              │
│                                                                         │
│  ④ 安全运维方                                                             │
│     ├─ 全程审计日志（ClickHouse 实时写入）                                  │
│     ├─ 链上存证（FISCO BCOS Merkle 根锚定）                                │
│     ├─ 安全告警（策略拒绝/异常操作/DP 预算耗尽）                             │
│     └─ 应急响应（密钥吊销/会话终止/数据回收）                                │
└─────────────────────────────────────────────────────────────────────────┘
```

**典型案例**：某银行使用政府企业纳税数据进行信贷风控评估
- 数据：某省企业纳税数据（500 万行，含企业 ID/纳税额/行业分类）
- 算法：信用评分模型（Python + scikit-learn）
- 输出：信用评分结果（聚合统计，ε=1.0 差分隐私保护）
- 安全：L1 TEE 沙箱，SM4 加密存储，链上存证

### 2.2 场景二：可信数据空间跨域安全流通

**背景**：多个异构可信数据空间（金融/政务/医疗/工业）需要互联互通，实现跨域数据价值流通。

**业务流程**：

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    跨域安全流通全流程                                      │
│                                                                         │
│  空间 A（金融数据空间）              空间 B（政务数据空间）                   │
│  ┌───────────────────┐            ┌───────────────────┐                │
│  │ 数商: 银行信贷数据  │            │ 数商: 企业纳税数据  │                │
│  │ 连接器 A           │            │ 连接器 B           │                │
│  └────────┬──────────┘            └────────┬──────────┘                │
│           │                                 │                          │
│           └─────────── 互联互通层 ──────────┘                          │
│                          │                                             │
│                 ┌────────▼────────┐                                    │
│                 │  密态沙箱系统 CDS │                                    │
│                 │  L1 TEE 强制     │                                    │
│                 │  联合计算模式     │                                    │
│                 └────────┬────────┘                                    │
│                          │                                             │
│                 ┌────────▼────────┐                                    │
│                 │ 输出审查网关     │                                    │
│                 │ 跨空间结果脱敏   │                                    │
│                 └─────────────────┘                                    │
│                                                                         │
│  买方（金融科技公司）                                                    │
│  ├─ 跨空间身份互认（SM2 证书链验证 → 联盟根 CA）                           │
│  ├─ 跨空间数据目录查询                                                   │
│  ├─ 签订跨空间合约（双方数商 + 买方三方签名）                               │
│  ├─ 联合计算沙箱（MPC + TEE 混合模式）                                    │
│  └─ 获取联合分析结果（仅聚合，无原始数据）                                  │
└─────────────────────────────────────────────────────────────────────────┘
```

**典型案例**：金融科技公司联合银行信贷数据 + 政府企业数据进行风控建模
- 数据源 A：银行信贷记录（空间 A，L1 TEE）
- 数据源 B：企业纳税数据（空间 B，L1 TEE）
- 计算模式：联合建模（MPC 两方计算 + TEE 可信聚合）
- 输出：风控模型权重（差分隐私 ε=2.0，不含训练数据）
- 存证：双空间审计日志 + 跨链存证（WeCross）

### 2.3 场景三：非结构化数据安全处理

**背景**：医疗影像、司法文书、工业质检图像等非结构化数据的安全流通。

**业务流程**：

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    非结构化数据安全处理流程                                 │
│                                                                         │
│  ① 数商上传                                                              │
│     ├─ 格式检测（MIME 魔数验证）                                          │
│     ├─ 恶意扫描（ClamAV）                                                │
│     ├─ SM4-GCM 加密存储                                                  │
│     └─ 元数据提取（pHash/标题/摘要向量）                                   │
│                                                                         │
│  ② TEE 内预处理                                                          │
│     ├─ 图像：人脸检测 → 打码 → pHash 提取                                 │
│     ├─ 文档：OCR → NER → 敏感词识别 → 遮盖                                │
│     ├─ 音频：ASR → 说话人分离 → 声纹脱敏                                   │
│     ├─ 视频：逐帧人脸模糊 → 车牌打码 → 行为分析                             │
│     └─ 特征向量加密存储（不可逆还原）                                       │
│                                                                         │
│  ③ 买方使用                                                              │
│     ├─ 选择处理后数据产品                                                 │
│     ├─ 沙箱内运行算法（图像分类/NLP/目标检测）                              │
│     └─ 输出：推理结果 JSON / 特征向量 / 分类标签                            │
│                                                                         │
│  ④ 输出审查                                                              │
│     ├─ 禁止原始文件输出                                                   │
│     ├─ 图像结果：分辨率压缩 + 不可见水印                                    │
│     ├─ 文档输出：仅摘要/关键词/分类标签                                     │
│     └─ 音视频输出：仅文字转录结果                                           │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.4 场景四：LLM 训练数据安全管线

**背景**：大模型训练需要海量高质量数据，但训练数据涉及隐私和版权问题。

**业务流程**：

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    LLM 训练数据安全管线                                    │
│                                                                         │
│  数据产品类型：                                                           │
│  ├─ 预训练语料（JSONL）→ 去重/清洗/分词/打包                               │
│  ├─ 指令微调数据（SFT）→ 质量过滤/PII 脱敏/格式转换                         │
│  ├─ 偏好数据（DPO/RLHF）→ 质量评估/配对/脱敏                               │
│  ├─ RAG 知识库（PDF/DOCX）→ 分块/向量化/索引                               │
│  └─ 多模态数据（图文对）→ 人脸打码/文字脱敏/配对                             │
│                                                                         │
│  沙箱内处理：                                                             │
│  ├─ PII 脱敏（Presidio + HanLP NER）                                     │
│  ├─ 质量过滤（困惑度/重复率/有害内容分类器）                                 │
│  ├─ 去重（MinHash LSH）                                                  │
│  ├─ 格式转换（Alpaca/ChatML/ShareGPT）                                   │
│  └─ 模型训练（LoRA 微调，差分隐私注入）                                     │
│                                                                         │
│  输出约束：                                                               │
│  ├─ 仅输出模型权重 + 训练指标                                              │
│  ├─ 禁止输出训练数据                                                      │
│  ├─ 模型安全检查（成员推断攻击防护）                                         │
│  └─ 模型水印嵌入（溯源）                                                   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.5 场景五：API 服务沙箱（在线推理）

**背景**：数商在沙箱内部署推理服务，买方通过 API 调用，原始数据不暴露。

```
数商部署模型 → 沙箱内加载（模型文件 SM4 加密存储）
买方调用 API → 输入经审查 → 沙箱内推理 → 输出经审查 → 返回结果
安全约束：
  - 模型参数不出沙箱
  - 输入/输出双向审查
  - 调用次数/频率配额控制
  - 推理结果差分隐私保护
```

### 2.6 场景六：数据产品开发沙箱

**背景**：数据提供商需要在安全环境中开发、测试、封装数据产品，而非仅消费已有数据。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    数据产品开发沙箱                                       │
│                                                                         │
│  ① 结构化数据开发场景                                                    │
│     ├─ 数据建模：沙箱内 ER 建模工具（dbt/Alembic）                        │
│     ├─ 安全存储：沙箱内 PostgreSQL 实例（数据隔离卷）                      │
│     ├─ 数据清洗：SQL/Python 脚本处理（缺失值/异常值/标准化）               │
│     ├─ 数据验证：约束检查 + 统计剖面分析                                  │
│     └─ 产品封装：Schema 导出 + 元数据生成 + SM4 加密打包                  │
│                                                                         │
│  ② 非结构化数据开发场景                                                  │
│     ├─ LLM 训练数据开发：                                                 │
│     │   ├─ 预训练语料：清洗/去重/分词/质量过滤                             │
│     │   ├─ SFT 指令数据：PII 脱敏/质量评估/格式转换                       │
│     │   ├─ RAG 知识库：文档分块/向量化/索引构建                           │
│     │   └─ 多模态数据：图文对标注/人脸打码/配对验证                        │
│     ├─ 视觉模型训练数据：                                                │
│     │   ├─ 图像分类：标注工具 + 数据增强 + 类别平衡                       │
│     │   ├─ 目标检测：边界框标注 + 难例挖掘                               │
│     │   └─ 图像分割：像素级标注 + 质量验证                                │
│     └─ 模型训练与评估：                                                  │
│         ├─ LoRA 微调（GPU 沙箱实例）                                     │
│         ├─ 模型评估（准确率/F1/困惑度）                                   │
│         └─ 模型导出（权重 + 配置 + 水印）                                 │
│                                                                         │
│  ③ 半结构化数据开发场景                                                  │
│     ├─ JSON/XML 日志处理：解析/提取/转换/聚合                             │
│     ├─ API 数据采集：沙箱内爬虫 + 速率限制 + 合规检查                     │
│     ├─ 数据转换：JSON↔CSV↔Parquet 格式互转                               │
│     └─ 数据集成：多源数据关联/合并/去重                                   │
│                                                                         │
│  ④ 安全约束                                                             │
│     ├─ 开发沙箱与生产数据物理隔离                                         │
│     ├─ 开发过程全程审计（代码版本/操作记录）                               │
│     ├─ 产品发布前安全审查（自动扫描 + 人工审核）                           │
│     └─ 开发者只能导出封装后的数据产品，不能导出原始数据                     │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 用户角色与旅程

### 3.1 数据提供商（数商）旅程

```
┌─────────────────────────────────────────────────────────────────┐
│  数商旅程                                                        │
│                                                                 │
│  ① 注册认证                                                      │
│     └─ 机构注册 → SM2 证书申请 → CA 颁发机构证书                    │
│                                                                 │
│  ② 数据产品上架                                                   │
│     ├─ 选择数据类型（结构化/非结构化/半结构化）                      │
│     ├─ 上传数据文件（SDK 加密上传 / API 批量导入）                  │
│     ├─ 配置元数据（字段描述/敏感级别/行业分类）                      │
│     ├─ 配置使用策略                                               │
│     │   ├─ 允许操作类型（聚合/过滤/模型训练/推理）                   │
│     │   ├─ 禁止操作（行级导出/网络访问/进程创建）                    │
│     │   ├─ 输出约束（最大行数/DP ε/格式限制）                       │
│     │   └─ 沙箱配置（最大运行时长/内存/语言）                       │
│     └─ 发布至数据目录                                              │
│                                                                 │
│  ③ 合约管理                                                      │
│     ├─ 审核买方合约申请（可配置自动审批规则）                        │
│     ├─ 签署数字合约（SM2 双方签名）                                 │
│     └─ 查看合约执行状态                                            │
│                                                                 │
│  ④ 使用监控                                                      │
│     ├─ 查看数据使用记录（谁/何时/用了什么/产出什么）                  │
│     ├─ 查看审计日志（链上存证验真）                                  │
│     ├─ 收益结算（按次/按量/包月）                                   │
│     └─ 数据更新/版本迭代                                           │
│                                                                 │
│  ⑤ 应急管理                                                      │
│     ├─ 主动吊销密钥（紧急数据回收）                                  │
│     ├─ 暂停数据产品（暂停所有会话）                                  │
│     └─ 查看安全告警（异常访问/策略违规）                             │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 数据使用方（买方）旅程

```
┌─────────────────────────────────────────────────────────────────┐
│  买方旅程                                                        │
│                                                                 │
│  ① 注册认证                                                      │
│     └─ 机构注册 → SM2 证书申请 → CA 颁发机构证书                    │
│                                                                 │
│  ② 数据发现                                                      │
│     ├─ 浏览数据目录（按行业/类型/敏感级别筛选）                      │
│     ├─ 查看数据产品质量报告（脱敏预览/完整率/分布统计）               │
│     ├─ 跨空间目录查询（互联互通场景）                                │
│     └─ 评估数据适用性                                             │
│                                                                 │
│  ③ 合约签订                                                      │
│     ├─ 选择合约类型（查询/训练/推理/联合建模）                       │
│     ├─ 协商合约条款（使用期限/操作范围/DP 参数/价格）                 │
│     ├─ 签署数字合约（SM2 双方签名）                                 │
│     └─ 支付（按次/按量/包月）                                      │
│                                                                 │
│  ④ 沙箱使用                                                      │
│     ├─ 创建沙箱会话                                               │
│     │   ├─ 系统验证合约有效性                                      │
│     │   ├─ TEE 远程证明（L1）或软件可信证明（L2）                    │
│     │   └─ 密钥分发至 TEE                                          │
│     ├─ 提交计算任务                                               │
│     │   ├─ 上传代码（SQL/Python/R，SM2 签名）                      │
│     │   ├─ 代码静态扫描（AST 白名单检查）                           │
│     │   └─ 沙箱内执行                                             │
│     └─ 获取结果                                                   │
│         ├─ 输出审查（DLP/DP/水印/签名）                             │
│         ├─ 结果加密传输                                           │
│         └─ 查看执行报告（DP 参数说明/审计事件 ID）                    │
│                                                                 │
│  ⑤ 结果使用                                                      │
│     ├─ 下载审查后结果                                             │
│     ├─ 验证结果签名（SM2 验真）                                    │
│     └─ 查看链上存证（可向监管证明合规）                              │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 运营方旅程

```
┌─────────────────────────────────────────────────────────────────┐
│  运营方旅程                                                       │
│                                                                 │
│  ① 平台配置                                                      │
│     ├─ 数据空间基础配置（名称/域名/CA 证书）                         │
│     ├─ 沙箱节点管理（L1/L2/L3 节点注册/下线/健康检查）               │
│     ├─ 跨空间连接器配置（外部空间接入/身份互认）                      │
│     └─ 合约模板管理（标准条款/定价策略）                             │
│                                                                 │
│  ② 合规管理                                                      │
│     ├─ 数据产品审核（上架前合规检查）                                │
│     ├─ 买方资质审核（行业许可/信用评估）                             │
│     ├─ 合规报告生成（TC609/等保/密评）                              │
│     └─ 监管接口对接（数据局/网信办）                                │
│                                                                 │
│  ③ 运营监控                                                      │
│     ├─ 资源监控（CPU/内存/存储/网络）                               │
│     ├─ 任务监控（队列长度/运行中/失败率）                            │
│     ├─ 安全监控（策略拒绝/异常操作/DP 预算）                         │
│     └─ 互联互通监控（跨空间会话/延迟/成功率）                        │
│                                                                 │
│  ④ 计量结算                                                      │
│     ├─ 使用量统计（按数商/买方/数据产品/时间）                       │
│     ├─ 账单生成                                                   │
│     └─ 收益分配                                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 3.4 安全运维方旅程

```
┌─────────────────────────────────────────────────────────────────┐
│  安全运维方旅程                                                    │
│                                                                 │
│  ① 安全配置                                                      │
│     ├─ 策略规则管理（OPA/Rego 策略包）                              │
│     ├─ 密钥生命周期管理（生成/轮转/吊销）                            │
│     ├─ 差分隐私参数配置（ε 预算/噪声机制）                           │
│     └─ 输出审查规则配置（DLP 规则/格式白名单）                       │
│                                                                 │
│  ② 安全监控                                                      │
│     ├─ 实时告警（策略违规/异常访问/密钥异常）                        │
│     ├─ 安全事件响应（会话终止/密钥吊销/节点隔离）                    │
│     ├─ 威胁情报（沙箱逃逸尝试/侧信道攻击检测）                      │
│     └─ 漏洞管理（依赖项扫描/TEE 补丁更新）                          │
│                                                                 │
│  ③ 审计溯源                                                      │
│     ├─ 审计日志查询（按 session/user/product/action 检索）           │
│     ├─ 链上存证验真（Merkle 证明验证）                              │
│     ├─ 争议仲裁导出（完整操作回放）                                  │
│     └─ 合规审计报告（自动生成 + 手动补充）                           │
│                                                                 │
│  ④ 应急响应                                                      │
│     ├─ 密钥紧急吊销（数据泄露应急）                                  │
│     ├─ 全局会话终止（安全事件响应）                                  │
│     ├─ 节点隔离（被攻陷节点下线）                                    │
│     └─ 事后分析（根因分析 + 修复方案）                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 系统架构设计

### 4.1 整体架构（控制平面/数据平面分离）

借鉴 Eclipse EDC 的控制平面/数据平面分离模式：

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              接入层                                      │
│  数商 SDK (Python/Java)  买方 SDK  管理控制台 (React)  跨空间连接器       │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │ HTTPS/TLCP（国密 TLS，Tongsuo）
┌──────────────────────────────▼──────────────────────────────────────────┐
│                              网关层                                      │
│  API Gateway (Kong/APISIX)  SM2 身份验证  合约验证中间件  限流/鉴权       │
└────────┬─────────────────────┬──────────────────────────────────────────┘
         │                     │
┌────────▼──────┐  ┌──────────▼───────────────────────────────────────────┐
│  控制平面      │  │              数据平面                                  │
│ Control Plane  │  │                                                      │
│                │  │  ┌──────────────────────────────────────────────┐   │
│ ┌────────────┐ │  │  │          沙箱运行时 Sandbox Runtime            │   │
│ │ 合约引擎    │ │  │  │                                              │   │
│ │ Contract   │ │  │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐    │   │
│ │ Engine     │ │  │  │  │ L1 TEE   │ │ L2 软件  │ │ L3 容器  │    │   │
│ │ (OPA+FISCO)│ │  │  │  │ SGX/SEV  │ │ gVisor   │ │ Docker   │    │   │
│ └────────────┘ │  │  │  └────┬─────┘ └────┬─────┘ └────┬─────┘    │   │
│ ┌────────────┐ │  │  │       └─────────────┴─────────────┘          │   │
│ │ 密钥服务    │ │  │  │              输出审查网关                      │   │
│ │ KMS        │ │  │  │  ┌──────────────────────────────────────┐   │   │
│ │ (Vault+HSM)│ │  │  │  │ DLP · DP · Watermark · NER · Sign    │   │   │
│ └────────────┘ │  │  │  └──────────────────────────────────────┘   │   │
│ ┌────────────┐ │  │  └──────────────────────────────────────────────┘   │
│ │ 策略编译器  │ │  │                                                      │
│ │ Policy     │ │  │  ┌──────────────────────────────────────────────┐   │
│ │ Compiler   │ │  │  │          任务调度器 Task Scheduler              │   │
│ └────────────┘ │  │  │  Celery + Pulsar · L1/L2/L3 路由 · 熔断       │   │
│ ┌────────────┐ │  │  └──────────────────────────────────────────────┘   │
│ │ 身份服务    │ │  │                                                      │
│ │ Identity   │ │  │                                                      │
│ │ (SM2 PKI)  │ │  │                                                      │
│ └────────────┘ │  │                                                      │
└───────────────┘  └──────────────────────────────────────────────────────┘
         │                     │
┌────────▼─────────────────────▼──────────────────────────────────────────┐
│                     存储与密钥层 Storage & Key Layer                      │
│  加密对象存储 (MinIO SM4-GCM)  KMS (Vault Transit + Tongsuo)  元数据库    │
│  (PostgreSQL)  缓存 (Redis)  消息队列 (Pulsar)                           │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────────────┐
│                     审计与存证层 Audit & Provenance Layer                  │
│  结构化日志 (ClickHouse)  日志收集 (Fluent Bit)  链上存证 (FISCO BCOS)    │
│  监控 (Prometheus + Grafana)  告警 (AlertManager)                        │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.2 架构设计决策

| 决策 | 选择 | 理由 | 参考 |
|------|------|------|------|
| 控制/数据平面分离 | OPA + FISCO (控制) / Sandbox Runtime (数据) | 解耦策略与执行，独立扩展 | Eclipse EDC |
| 策略引擎 | OPA (Rego) | 声明式策略，云原生生态成熟，REST API | OPA 社区 |
| 区块链 | FISCO BCOS | 国产联盟链，WeBank 开源，性能优异 | FISCO BCOS |
| KMS | Vault Transit + Tongsuo | 信封加密 + 国密支持 | HashiCorp Vault |
| TEE 框架 | Occlum (SGX) / Gramine (备选) | LibOS 模式，应用无需修改 | 蚂蚁集团 Occlum |
| VM 隔离 | Firecracker + gVisor | 双层隔离，AWS 验证 | AWS Firecracker |
| 任务调度 | Celery + Pulsar | 成熟分布式任务框架 + 高性能消息 | Apache Pulsar |
| DLP | Microsoft Presidio | 开源 PII 检测，可扩展自定义识别器 | Microsoft |
| 差分隐私 | IBM diffprivlib | 预算追踪，Laplace/Gaussian 机制 | IBM Research |
| 中文 NER | HanLP | 36k stars，中文 NER SOTA | HanLP |
| 中文 OCR | PaddleOCR | 百度开源，中文识别最佳 | PaddlePaddle |
| 中文 ASR | FunASR (Paraformer) | 阿里开源，中文 ASR 最佳 | ModelScope |
| 人脸检测 | InsightFace (SCRFD) | 高精度 + 轻量 | InsightFace |
| API 框架 | FastAPI | 异步 + 自动文档 + Pydantic 校验 | FastAPI |
| TLCP | Tongsuo | 国密 TLS，OpenSSL 兼容 | 铜锁 |
| 跨链 | WeCross | 微众银行跨链平台 | WeBank |

### 4.3 核心组件清单

| 组件 | 技术选型 | 职责 | 部署模式 |
|------|----------|------|----------|
| API 网关 | Kong / APISIX | 限流、鉴权、路由、日志 | K8s Deployment |
| 身份服务 | 自研 SM2 PKI + Vault PKI | SM2 证书颁发、验证、吊销 | K8s Deployment |
| 合约服务 | 自研 + OPA + FISCO BCOS | 数字合约全生命周期管理 | K8s Deployment |
| 密钥服务 (KMS) | Vault Transit + Tongsuo + HSM | SM4 密钥生成、分发、吊销 | K8s StatefulSet |
| 策略编译器 | 自研 (Rego 方言) | 合约条款 → 可执行策略包 | K8s Deployment |
| 任务调度器 | Celery + Pulsar | 任务队列、节点选择、熔断 | K8s Deployment |
| L1 沙箱运行时 | Occlum (SGX) / Gramine | TEE 硬件隔离执行环境 | K8s DaemonSet (SGX 节点) |
| L2 沙箱运行时 | Firecracker + gVisor | 软件增强隔离执行环境 | K8s DaemonSet |
| L3 沙箱运行时 | Docker + Seccomp-BPF | 最小化容器隔离 | K8s DaemonSet |
| 输出审查网关 | 自研 + Presidio + diffprivlib | 结果合规过滤 + 脱敏 | K8s Deployment |
| 加密对象存储 | MinIO (SM4 服务端加密) | 密态数据产品存储 | K8s StatefulSet |
| 元数据库 | PostgreSQL 15 | 产品目录、会话状态、策略 | K8s StatefulSet |
| 缓存 | Redis 7 | Session、配额、热数据 | K8s StatefulSet |
| 审计日志 | ClickHouse | 细粒度操作日志存储 | K8s StatefulSet |
| 链上存证 | FISCO BCOS | 日志摘要不可篡改存证 | 独立部署 |
| 消息队列 | Apache Pulsar | 任务事件、审计流 | K8s StatefulSet |
| 监控 | Prometheus + Grafana + Loki | 指标、告警、日志可观测 | K8s Deployment |
| 日志收集 | Fluent Bit | 审计日志收集 | K8s DaemonSet |

---

## 5. 功能模块详细设计

### 5.1 F1：数据产品管理

**职责**：数商通过此模块将数据资产封装为沙箱可消费的标准数据产品。

**子模块**：

| 子模块 | 功能 | 优先级 |
|--------|------|--------|
| 产品注册 | 元数据填写（名称/类型/字段/行业/量级）+ 目录发布 | P0 |
| 密态封装 | SM4-GCM 加密 + 信封加密 DEK 托管 KMS | P0 |
| 质量预检 | TEE 内脱敏预览 + 质量报告生成 | P1 |
| 策略配置 | 操作白名单/禁止操作/输出限制/DP 参数 | P0 |
| 版本管理 | 数据产品版本迭代 + 历史版本审计 | P2 |
| 下架回收 | 合约到期密钥吊销 + 历史存证保留 | P1 |

**数据产品元数据模型**：

```yaml
DataProduct:
  id: "dp-{uuid}"
  name: "某省企业纳税数据2023"
  category: "政务·财税"
  dataType: "structured"           # structured | semi_structured | unstructured
  securityLevel: "L1"              # L1 | L2 | L3
  schema:
    - field: "tax_id"
      type: "string"
      sensitivity: "high"          # high | medium | low
    - field: "revenue_range"
      type: "enum"
      sensitivity: "medium"
  volumeRows: 5000000
  updateFrequency: "monthly"
  allowedOperations:
    - "aggregate"
    - "filter"
    - "statistical_model"
  forbiddenOperations:
    - "row_level_export"
    - "join_with_external"
    - "network_call"
  outputConstraints:
    maxOutputRows: 100
    requireDifferentialPrivacy: true
    epsilon: 1.0
  sandboxConfig:
    maxRuntimeSeconds: 3600
    maxMemoryGB: 16
    allowedLanguages: ["python", "sql"]
  contractRequired: true
  certifications: ["TC609-2025", "数安标志"]
```

**技术实现要点**：
- 信封加密：Vault Transit `generate_data_key` → 明文 key 仅在 TEE 内使用
- 元数据存储：PostgreSQL JSONB 字段存储 schema + 策略
- 目录索引：Elasticsearch（全文检索）+ PostgreSQL（结构化查询）
- 上传通道：分块上传 + SM4-GCM 流式加密 + SM3 完整性校验

### 5.2 F2：沙箱运行环境

**职责**：密态沙箱的核心计算容器，提供硬件级隔离的安全执行环境。

**沙箱类型**：

| 类型 | 适用场景 | 计算模式 | 隔离级别 | 启动延迟 |
|------|----------|----------|----------|----------|
| **标准分析沙箱** | SQL 查询、统计分析 | 单任务独占 TEE | L1 TEE (SGX) | ≤60s |
| **模型训练沙箱** | 机器学习、深度学习 | 批处理长任务 | L1 TEE (SEV) | ≤60s |
| **联合计算沙箱** | 多数商数据联合分析 | MPC + TEE 混合 | L1 分布式 | ≤120s |
| **API 服务沙箱** | 买方调用推理服务 | 在线推理 | L1/L2 轻量 | ≤15s |
| **多模态分析沙箱** | 图文混合数据训练 | GPU-TEE | L1 (H100 CC) | ≤120s |
| **流媒体沙箱** | 实时音视频流分析 | 流式处理 | L2 | ≤15s |
| **文档理解沙箱** | 合同审查、知识图谱 | NLP 流水线 | L1/L2 | ≤30s |

**沙箱生命周期**（借鉴 Eclipse EDC 数据传输生命周期）：

```
[合约激活] → [密钥分发至 TEE] → [数据解密入 TEE 内存]
     ↓
[买方算法/SQL 上传并验签] → [TEE 内执行] → [输出审查]
     ↓
[结果返回买方] → [任务日志存证] → [TEE 销毁/密钥吊销]
```

**关键设计**：
- **虚拟路径隐藏**：数据挂载在 `/data`（只读），工作区在 `/workspace`（可写），隐藏宿主路径
- **网络隔离**：L1 默认禁止外联，仅允许与审计服务的回环通信
- **资源限制**：cgroup CPU/内存限制 + 超时熔断
- **代码验证**：AST 白名单 + RestrictedPython 沙箱编译

### 5.3 F3：互联互通接口

**职责**：支持接入多个异构可信数据空间，实现跨空间数据产品消费。

**标准接口**（对齐 TC609 / NDI-TR-2025-02）：

| 接口 | 方法 | 描述 |
|------|------|------|
| `/sandbox/register` | POST | 向服务平台注册沙箱实例 |
| `/sandbox/session/create` | POST | 基于已签合约创建沙箱会话 |
| `/sandbox/task/submit` | POST | 提交计算任务 |
| `/sandbox/task/result` | GET | 拉取经审查的计算结果 |
| `/sandbox/attest` | GET | 获取 TEE 远程证明报告 |
| `/sandbox/audit/log` | GET | 查询操作审计日志 |
| `/sandbox/contract/bind` | POST | 绑定数字合约 |
| `/crossspace/catalog/query` | GET | 跨空间数据产品目录查询 |
| `/crossspace/identity/verify` | POST | 跨空间身份互认 |

**跨空间身份互认协议**（借鉴 IDS DSP）：

```
空间 A（含 CA-A）                    空间 B（含 CA-B）
     │                                     │
     │  1. 空间联盟根 CA 签发互信证书         │
     │ ←─────── Root CA 签名 ──────────────→│
     │                                     │
买方（空间B）        ──请求访问空间A数据→    空间A连接器
     │                                     │
     │  2. 提交 SM2 证书（CA-B 签发）        │
     │ ──────────────────────────────────→ │
     │                                     │ 3. 验证证书链
     │  4. 返回跨空间临时访问令牌（SM2 签名） │
     │ ←──────────────────────────────────  │
     │                                     │
     │  5. 携带令牌创建沙箱会话              │
     │ ──────────────────────────────────→ │
```

### 5.4 F4：使用控制引擎

**职责**：对沙箱内所有数据操作实施实时策略控制。

**控制维度**：

| 维度 | 控制内容 | 执行方式 | 参考 |
|------|----------|----------|------|
| 操作控制 | 允许/禁止特定 API 调用 | Seccomp/TEE syscall 过滤 | gVisor Seccomp |
| 时间控制 | 最大运行时长、访问时间窗口 | 任务调度器 + 定时熔断 | Celery timeout |
| 数量控制 | 最大查询行数、最大输出量、最大任务次数 | Redis 原子计数器 | AWS Clean Rooms 配额 |
| 字段控制 | 高敏感字段 TEE 内自动脱敏 | 字段级策略引擎 | OPA Rego |
| 模型控制 | 禁止模型参数/梯度输出 | 差分隐私注入 | IBM diffprivlib |
| 身份控制 | 仅合约指定买方可创建会话 | SM2 身份验证 | Vault PKI |

**策略执行架构**（借鉴 OPA 策略即代码）：

```
合约签署 → 策略编译器（Rego）→ 策略包（加密下发至 TEE）
                              ↓
          用户操作 → 策略执行引擎（TEE 内 OPA）
                              ↓
          ┌──────────────────────────────────┐
          │ Permit → 执行操作 → 记录日志       │
          │ Deny  → 返回拒绝原因 → 告警       │
          │ 连续拒绝 ≥5 次 → 触发安全告警      │
          └──────────────────────────────────┘
```

### 5.5 F5：输出审查网关

**职责**：沙箱唯一输出通道，对所有离开 TEE 的结果进行安全审查。

**审查流水线**（6 阶段）：

```
TEE 计算结果（内存缓冲区）
    │
    ▼
① 格式验证（python-magic MIME 检测）
    • 允许：JSON/CSV/文字/标签
    • 拒绝：图像/音频/视频/二进制
    │
    ▼
② 内容安全扫描（Presidio + HanLP NER）
    • PII 检测（姓名/手机/身份证/银行卡）
    • 企业机密信息检测
    • 原始数据重建检测（字段精确匹配率 > 阈值 → 拒绝）
    │
    ▼
③ 差分隐私处理（diffprivlib）
    • 全局敏感度分析
    • Laplace 噪声注入：noise ~ Lap(Δf / ε)
    • ε 预算耗尽 → 拒绝查询
    │
    ▼
④ 输出量限制
    • 行数计数器校验（≤ maxOutputRows）
    • 字节大小校验（≤ maxOutputBytes）
    │
    ▼
⑤ 溯源水印注入
    • 文字：零宽字符隐写（ZWSP/ZWNJ 编码 session_id）
    • 数值：LSB 编码（精度影响 < 0.001%）
    │
    ▼
⑥ SM2 签名输出
    • 沙箱节点私钥签名
    • 签名随结果返回，供事后验真
    │
    ▼
合规输出（TLCP 加密传输 → 买方）
```

**差分隐私预算管理**（借鉴 AWS Clean Rooms）：

```
每个合约维护一个 ε 预算账本：

contract.dpBudget.total = ε_total      # 合约总预算
contract.dpBudget.consumed = 0          # 已消耗预算

每次查询：
    ε_cost = estimate_query_sensitivity(query)
    if consumed + ε_cost > total:
        → 拒绝查询，提示预算耗尽
    else:
        → 执行查询，注入 Laplace(Δf / ε_cost) 噪声
        → consumed += ε_cost
        → 写入审计日志
```

### 5.6 F6：存证审计中心

**职责**：全生命周期操作细粒度记录 + 区块链不可篡改存证。

**审计架构**：

```
TEE 沙箱内                              TEE 外
────────────────────────                 ─────────────────
操作发生
    │
    ▼
PolicyEngine 判定 → 生成审计事件（JSON）
    │
    │ SM2 签名（沙箱私钥）
    ▼
[日志收集器 Fluent Bit]
    │
    ├─→ ClickHouse（实时写入，保留 3 年+）
    │
    └─→ 上链流水线
            │ 批量聚合（每 1000 条或每 30 秒）
            ▼
        SM3(batch_events) → Merkle 根哈希
            │
            ▼
        FISCO BCOS 合约调用：
        AuditRegistry.submitBatch(merkleRoot, batchId, timestamp)
            │
            ▼
        链上存证（不可篡改，永久保存摘要）
```

**审计日志字段**：

```json
{
  "event_id": "evt-{uuid}",
  "timestamp": "2026-06-01T10:23:45.123Z",
  "sandbox_session_id": "sess-{uuid}",
  "contract_id": "contract-{uuid}",
  "actor": {
    "space_id": "space-gov-A",
    "org_id": "org-fintech-001",
    "user_id": "user-sm2-cert-fingerprint",
    "role": "data_buyer"
  },
  "action": "task_submit",
  "resource": {
    "product_id": "dp-xxx",
    "data_version": "v2.1"
  },
  "policy_check": {
    "result": "allow",
    "matched_rule": "rule-003-allow-aggregate"
  },
  "output": {
    "row_count": 45,
    "dp_epsilon": 1.0,
    "sensitive_fields_redacted": ["mobile", "id_card"]
  },
  "tee_attestation_hash": "3f2a...b9c1",
  "blockchain_tx": "0xabc...123"
}
```

### 5.7 F7：身份与密钥管理

**职责**：基于国密 SM2 的身份体系 + 数据产品绑定的密钥生命周期管理。

**身份体系**：

| 实体 | 证书类型 | 签发机构 | 有效期 |
|------|----------|----------|--------|
| 数商机构 | SM2 X.509 机构证书 | 数据空间运营方 CA | 1 年 |
| 买方机构 | SM2 X.509 机构证书 | 数据空间运营方 CA | 1 年 |
| 沙箱实例 | TEE 平台证书 + 运行时证明 | Intel/AMD/国产硬件 | 动态 |
| 跨空间互认 | 跨空间 SM2 互信证书 | 数据空间联盟根 CA | 2 年 |

**密钥生命周期**：

```
数商上传数据 → SM4 密钥加密数据 → 密钥托管至 KMS（HSM 保护）
                                     ↓
合约激活 → KMS 验证合约有效性 → 向 TEE 内安全分发 SM4 密钥
                                     ↓
TEE 内解密使用 → 合约到期/任务结束 → TEE 销毁密钥材料
                                     ↓
数商主动吊销 → KMS 吊销密钥 → 所有关联沙箱会话立即终止
```

**密钥层次结构**：

```
Root CA 私钥（离线，HSM 保护）
  │
  ├── 中间 CA（KMS 服务证书）
  │     └── 节点证书（各沙箱节点 SM2 证书）
  │
  ├── 数商身份证书（SM2）
  │     └── 数据产品密钥 DEK（SM4-256，per-product）
  │
  ├── 买方身份证书（SM2）
  │     └── 会话密钥 Session Key（SM4，per-session，≤24h）
  │
  └── 沙箱节点证书（SM2 + TEE 证书链）
        └── 内存加密密钥 MEK（L1: CPU 硬件派生 / L2: KMS 分发）
```

### 5.8 F8：数字合约集成

**职责**：以合约为驱动激活/约束沙箱行为。

**合约类型**：

| 合约类型 | 描述 | 输出 |
|----------|------|------|
| 数据查询合约 | 买方提交 SQL，返回脱敏聚合结果 | 聚合表格 |
| 模型训练合约 | 买方上传算法在数商数据上训练 | 模型文件（不含原始数据） |
| API 服务合约 | 数商在沙箱部署推理服务 | 推理结果 |
| 联合建模合约 | 多数商数据联合计算 | 联合模型/特征 |

**合约条款 → 沙箱行为映射**：

| 合约条款 | 沙箱行为 |
|----------|----------|
| 使用期限 | 超期后密钥自动吊销，会话终止 |
| 允许操作类型 | 映射为 OPA 策略包 |
| 差分隐私参数 ε | 输出审查网关注入对应噪声级别 |
| 允许访问用户范围 | 身份验证时限定用户列表 |
| 结果输出限制 | 行数/体积上限配置到输出网关 |
| 争议解决条款 | 审计日志自动保留 N 天供仲裁 |

**合约状态机**：

```
DRAFT → PENDING_APPROVAL → APPROVED → SIGNED → ACTIVE
  → SUSPENDED / EXPIRED → TERMINATED → ARCHIVED
```

### 5.9 F9：监控运维中心

**职责**：为运营方提供可观测视图（运维人员不可访问沙箱内数据）。

**监控指标**：

| 类别 | 指标 |
|------|------|
| 资源 | CPU 利用率、内存占用、存储配额消耗、GPU 利用率 |
| 任务 | 队列长度、运行中任务数、失败率、平均执行时长 |
| 安全 | 策略拒绝次数、异常操作告警、输出审查拦截次数 |
| 互联互通 | 跨空间会话数、证明报告验证成功率、跨空间延迟 |
| 合约 | 活跃合约数、即将到期合约、合约违约告警 |
| DP 预算 | 各合约 ε 消耗/剩余、预算耗尽预警 |

---

## 6. 安全架构设计

### 6.1 三级安全体系

```
┌─────────────────────────────────────────────────────────────────┐
│              沙箱安全等级体系（L1 → L3）                          │
│                                                                 │
│  L1  TEE 沙箱（标准级）                                          │
│  ──────────────────────────────────────────────                 │
│  • 硬件隔离（Intel SGX / AMD SEV / 国产 TEE）                    │
│  • 远程证明（DCAP Attestation）                                  │
│  • 内存全程加密（TME/SME）                                       │
│  • 适用：重要/核心数据分类                                        │
│  • 参考：Gramine SGX、Occlum LibOS                              │
│                                                                 │
│  L2  软件增强沙箱（降级级）                                       │
│  ──────────────────────────────────────────────                 │
│  • Firecracker microVM + gVisor 双层隔离                        │
│  • Seccomp-BPF 系统调用过滤（~42 个白名单）                       │
│  • MPC 双方验证（弥补硬件信任根缺失）                              │
│  • 更严格差分隐私（ε≤0.5）                                      │
│  • 适用：一般敏感数据                                            │
│                                                                 │
│  L3  最小化隔离沙箱（兜底级）                                     │
│  ──────────────────────────────────────────────                 │
│  • Docker + Seccomp-BPF + AppArmor                              │
│  • 全程同态加密（PHE，仅加法/乘法）                               │
│  • 输出仅允许聚合统计                                            │
│  • 适用：低敏/公开数据                                           │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 威胁模型（STRIDE）

| 威胁 | 具体威胁 | 对抗措施 |
|------|----------|----------|
| Spoofing | 伪造数商/买方身份 | SM2 证书 + MFA；跨空间令牌时间戳防重放 |
| Tampering | 修改策略包、审计日志 | 策略包 SM3 哈希锁定；日志 SM2 签名；链上存证 |
| Repudiation | 否认执行过操作 | 链上不可篡改存证；操作日志含 SM2 签名 |
| Info Disclosure | 数据从 TEE 内泄露 | 硬件内存加密；输出审查；差分隐私；禁止原文输出 |
| DoS | 消耗 TEE 资源 | 任务配额；cgroup 限制；超时熔断 |
| Elevation | 沙箱逃逸 | TEE 硬件隔离；gVisor；Seccomp-BPF |

### 6.3 侧信道防护（L1 专项）

| 攻击 | 防护 |
|------|------|
| 缓存侧信道 | Occlum 缓存分区；禁用超线程 |
| 内存访问模式 | ORAM（高安全场景选用） |
| 时序侧信道 | 恒定时间 SM2 实现 |
| 功耗侧信道 | 云环境已缓解；物理机需电磁屏蔽 |

### 6.4 国密算法使用矩阵

| 用途 | 算法 | 标准 |
|------|------|------|
| 数字签名 | SM2-256 | GB/T 32918 |
| 密钥交换 | SM2-ECDH | GB/T 32918 |
| 消息摘要 | SM3-256 | GB/T 32905 |
| 数据加密（存储） | SM4-GCM | GB/T 32907 |
| 数据加密（传输） | SM4-CBC | GB/T 32907 |
| 传输层协议 | TLCP | GM/T 0024 |

---

## 7. 业务闭环设计

### 7.1 完整业务闭环

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         CDS 完整业务闭环                                  │
│                                                                         │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐         │
│  │ 数据上架  │───→│ 合约签订  │───→│ 沙箱执行  │───→│ 结果输出  │         │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘         │
│       │               │               │               │                │
│       ▼               ▼               ▼               ▼                │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐         │
│  │ 密态封装  │    │ 策略编译  │    │ TEE 隔离  │    │ 输出审查  │         │
│  │ 元数据    │    │ SM2 签名  │    │ 密钥分发  │    │ DP 注入  │         │
│  │ 质量报告  │    │ 链上存证  │    │ 策略执行  │    │ 水印签名  │         │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘         │
│       │               │               │               │                │
│       └───────────────┴───────────────┴───────────────┘                │
│                              │                                          │
│                       ┌──────▼──────┐                                   │
│                       │  审计存证    │                                   │
│                       │  ClickHouse │                                   │
│                       │  FISCO BCOS │                                   │
│                       └──────┬──────┘                                   │
│                              │                                          │
│                       ┌──────▼──────┐                                   │
│                       │  监控告警    │                                   │
│                       │  Prometheus │                                   │
│                       │  Grafana    │                                   │
│                       └─────────────┘                                   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 7.2 数据产品全生命周期

```
创建 → 加密封装 → 质量预检 → 目录发布 → 合约绑定 → 沙箱使用
  → 结果输出 → 审计存证 → 版本更新 → 下架回收 → 密钥吊销
```

### 7.3 沙箱会话全生命周期

```
合约验证 → 远程证明 → 密钥分发 → 数据加载 → 代码验证
  → 策略执行 → 计算运行 → 输出审查 → 结果签名 → 密钥销毁
  → 审计上链 → 会话关闭
```

### 7.4 合约全生命周期

```
草稿 → 提交审批 → 审批通过 → 双方签署 → 合约激活
  → 沙箱会话创建 → 任务执行 → 结果交付 → 合约到期
  → 密钥吊销 → 审计归档
```

---

## 8. 部署架构

### 8.1 单空间部署（最小生产配置）

```
┌─────────────────────────────────────────────────────────┐
│                  可信数据空间 A                            │
│                                                         │
│  ┌────────────────────┐  ┌──────────────────────┐      │
│  │    控制平面节点      │  │    TEE 计算节点（L1）  │      │
│  │  2C8G 常规服务器    │  │  Intel Xeon SGX 服务器│      │
│  │                    │  │  EPC: 256GB+         │      │
│  │  • API 网关         │  │  • Occlum 运行时      │      │
│  │  • 合约服务         │  │  • DCAP 证明服务      │      │
│  │  • 密钥服务(KMS)    │  │  • 任务执行池         │      │
│  │  • 任务调度器       │  └──────────────────────┘      │
│  │  • 审计服务         │                                │
│  └────────────────────┘  ┌──────────────────────┐      │
│                          │  L2 降级计算节点       │      │
│  ┌────────────────────┐  │  普通 x86 服务器      │      │
│  │    存储节点         │  │  • Firecracker       │      │
│  │  • MinIO（加密）    │  │  • gVisor            │      │
│  │  • PostgreSQL       │  │  • eBPF 监控         │      │
│  │  • Redis            │  └──────────────────────┘      │
│  └────────────────────┘                                │
│                                                         │
│  ┌────────────────────┐                                │
│  │    区块链节点        │                                │
│  │  • FISCO BCOS      │                                │
│  └────────────────────┘                                │
└─────────────────────────────────────────────────────────┘
```

### 8.2 跨空间互联互通部署

```
空间 A（金融数据空间）              空间 B（政务数据空间）
┌─────────────────────┐          ┌─────────────────────┐
│  连接器 A           │          │  连接器 B            │
│  • 服务平台适配      │ ←─TLCP─→ │  • 服务平台适配       │
│  • 身份转换层        │          │  • 身份转换层         │
│  • 数据目录代理      │          │  • 数据目录代理       │
└──────────┬──────────┘          └──────────┬──────────┘
           │                                │
           └──────────┬─────────────────────┘
                      │
           ┌──────────▼──────────┐
           │  密态沙箱（CDS）     │
           │  L1 节点（跨空间     │
           │  互联互通强制 L1）   │
           └─────────────────────┘
```

---

## 9. 实现计划

### 9.1 Phase 1（0-6 个月）：核心沙箱能力

**Sprint 1-2（月 1）：基础设施**

| 模块 | 任务 | 优先级 |
|------|------|--------|
| F7 身份 | SM2 PKI 证书颁发/验证（Tongsuo + Vault PKI） | P0 |
| F7 密钥 | KMS 密钥生命周期（Vault Transit + SM4） | P0 |
| 基础设施 | PostgreSQL Schema + Redis + ClickHouse | P0 |
| 基础设施 | API Gateway (Kong) + SM2 JWT 认证 | P0 |

**Sprint 3-4（月 2）：合约 + 策略**

| 模块 | 任务 | 优先级 |
|------|------|--------|
| F8 合约 | 合约状态机（transitions 库） | P0 |
| F8 合约 | OPA 策略编译器（合约 → Rego） | P0 |
| F4 控制 | 策略执行引擎（OPA REST API 集成） | P0 |
| F6 审计 | ClickHouse 审计 Schema + structlog | P0 |

**Sprint 5-6（月 3）：L3 沙箱**

| 模块 | 任务 | 优先级 |
|------|------|--------|
| F2 沙箱 | L3 Docker + Seccomp 沙箱运行时 | P0 |
| F2 沙箱 | 买方代码验证（AST 白名单 + RestrictedPython） | P0 |
| F5 输出 | 基础输出审查（格式验证 + PII 正则） | P0 |
| F1 产品 | 数据产品注册 + SM4 加密封装 | P0 |

**Sprint 7-8（月 4）：L1 TEE 沙箱**

| 模块 | 任务 | 优先级 |
|------|------|--------|
| F2 沙箱 | L1 Occlum SGX 沙箱运行时 | P0 |
| F2 沙箱 | DCAP 远程证明集成 | P0 |
| F2 沙箱 | TEE 内密钥安全分发 | P0 |
| F5 输出 | Presidio DLP + HanLP NER 集成 | P0 |

**Sprint 9-10（月 5）：差分隐私 + 存证**

| 模块 | 任务 | 优先级 |
|------|------|--------|
| F5 输出 | diffprivlib 差分隐私注入 + 预算管理 | P0 |
| F5 输出 | 水印注入（Unicode + LSB） | P1 |
| F6 审计 | FISCO BCOS 存证合约 + Merkle 树 | P0 |
| F6 审计 | SM2 签名审计日志 | P0 |

**Sprint 11-12（月 6）：集成测试 + 合规**

| 模块 | 任务 | 优先级 |
|------|------|--------|
| 集成 | 端到端流程测试（数据上架→合约→沙箱→输出） | P0 |
| 集成 | L2 Firecracker + gVisor 沙箱 | P1 |
| 合规 | 等保三级预评估 | P1 |
| 运维 | Prometheus + Grafana 监控大盘 | P1 |

### 9.2 Phase 2（6-12 个月）：互联互通能力

| Sprint | 任务 |
|--------|------|
| 13-14 | TC609 标准接口全面实现 |
| 15-16 | 跨空间身份互认（SM2 证书链 + 联盟根 CA） |
| 17-18 | 数字合约驱动的沙箱激活 |
| 19-20 | 区块链存证中心上线 |
| 21-22 | 差分隐私输出模块完善 |
| 23-24 | 集成测试 + TC609 互操作认证 |

### 9.3 Phase 3（12-18 个月）：高级沙箱能力

| 任务 | 描述 |
|------|------|
| 联合计算沙箱 | MPC + TEE 混合模式（MP-SPDZ 集成） |
| 模型训练沙箱 | 大内存 TEE 支持 + GPU-TEE (H100 CC) |
| API 服务沙箱 | 在线推理 + 模型安全检查 |
| 非结构化管线 | 图像/文档/音视频全类型处理 |
| LLM 训练管线 | 预训练语料/SFT/RAG/多模态处理 |
| 多空间联合审计 | WeCross 跨链审计 |
| 监管接口 | 数据局/网信办对接 |

### 9.4 Phase 4（18-24 个月）：生态扩展

| 任务 | 描述 |
|------|------|
| 多空间类型 | 企业/行业/城市/个人/跨境 5 类数据空间 |
| 大模型融合 | 数据增强、联邦精调 |
| 自动化合规 | 合规报告自动生成 |
| 计量结算 | 数据产品定价 + 使用量计量 + 收益分配 |

---

## 10. 风险与应对

### 10.1 技术风险

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| TEE 硬件可用性 | 部分服务器无 SGX/SEV | 三级安全体系 L1/L2/L3 自动降级 |
| TEE 性能损耗 | EPC 内存限制，大文件处理慢 | 分块流式处理 + GPU-TEE 加速 |
| 国密算法生态 | 部分库不支持 SM2/SM4 | Tongsuo 作为系统 OpenSSL 替换 |
| MPC 通信开销 | 两方计算增加网络延迟 | 预计算乘法三元组，O(1) 通信 |
| 区块链性能 | FISCO BCOS TPS 限制 | 仅存 Merkle 根，不存原始日志 |
| 侧信道攻击 | TEE 已知漏洞 | 缓存分区 + 禁用超线程 + ORAM |

### 10.2 业务风险

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| 合规标准变更 | TC609 标准更新 | 模块化设计，策略引擎可热更新 |
| 市场竞争 | 蚂蚁隐语等竞品 | 差异化：TC609 合规 + 互联互通 + 国密全覆盖 |
| 用户接受度 | 数商不愿上架敏感数据 | 先从政务公共数据切入，建立信任 |
| 跨空间协调 | 多空间标准不统一 | 对齐 NDI-TR-2025-02 互联互通标准 |

### 10.3 安全风险

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| TEE 0-day 漏洞 | 沙箱逃逸 | L2/L3 降级 + 应急密钥吊销 |
| 密钥泄露 | 数据解密 | HSM 保护 + 短生命周期 + 自动轮转 |
| 内部人员攻击 | 运维人员越权 | 零信任运维 + 操作录屏 + 双人审核 |
| 差分隐私攻击 | 多次查询重建数据 | ε 预算管理 + 查询审计 + 重建检测 |

---

*文档版本：v1.0 | 状态：初稿 | 作者：@架构师 | 基于 specs v1.1 + 开源技术调研*
