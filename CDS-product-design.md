# 密态沙箱系统（CDS）· 产品设计文档

> 版本：v1.0
> 状态：设计完成
> 基于：product-spec v1.1 + detailed-tech-spec v2.0 + frontend-spec v1.0 + data-security-spec + distributed-sandbox-connector-spec + secure-output-lifecycle + advanced-scenarios-spec + frontend-roles-design
> 对齐标准：TC609-6-2025-01、NDI-TR-2025-02、GM/T 国密系列、中国信通院《可信数据空间技术架构》、《数据安全流通技术规范》、《隐私计算技术规范》、《数据沙箱技术规范》
> 参考项目：Eclipse EDC、GAIA-X、SecretFlow、Gramine、Confidential Containers (CoCo)、OpenDP、PySyft

---

## 目录

1. [产品概述与定位](#1-产品概述与定位)
2. [核心场景设计](#2-核心场景设计)
3. [用户角色与完整业务闭环](#3-用户角色与完整业务闭环)
4. [功能模块详细设计](#4-功能模块详细设计)
5. [技术架构与开源选型](#5-技术架构与开源选型)
6. [安全架构](#6-安全架构)
7. [系统集成与部署](#7-系统集成与部署)
8. [实施路线图](#8-实施路线图)

---

## 1. 产品概述与定位

### 1.1 产品定义

密态沙箱系统（Confidential Data Sandbox，CDS）是面向可信数据空间互联互通场景的密态数据流通基础设施。通过硬件级隔离 + 密码学控制 + 数字合约约束，实现：

> **"数据进、算法进、结果出——原始数据不出域，价值安全流通"**

### 1.2 三大安全承诺

| 承诺 | 含义 | 技术保障 |
|------|------|----------|
| **可用不可见** | 数据在密态环境中被使用，任何人（含运维）无法直接访问原文 | TEE 硬件隔离 + 内存加密 |
| **可出不可取** | 仅符合合约约定的计算结果可离开沙箱，原始数据无法导出 | 输出控制网关 + 差分隐私脱敏 |
| **可审不可改** | 所有操作行为均被细粒度记录并链上存证，不可篡改 | 区块链存证 + 远程证明 |

### 1.3 产品定位

```
┌─────────────────────────────────────────────────────────┐
│                  可信数据空间生态                          │
│                                                         │
│  数商（提供方）  ←──数字合约──→  买方（使用方）            │
│       ↓                               ↓                 │
│  数据产品封装                    算法/应用上传             │
│       ↓                               ↓                 │
│  ┌─────────────────────────────────────────┐            │
│  │          密态沙箱系统（CDS）              │            │
│  │   TEE 隔离计算 · 使用控制 · 结果脱敏      │            │
│  │   远程证明 · 链上存证 · 互联互通接口       │            │
│  └─────────────────────────────────────────┘            │
│       ↓                                                  │
│  服务平台（合约管理 / 存证 / 计量 / 目录）                  │
│       ↓                                                  │
│  跨空间互联互通（其他可信数据空间）                          │
└─────────────────────────────────────────────────────────┘
```

### 1.4 设计原则

| 原则 | 说明 | 参考 |
|------|------|------|
| **控制平面/数据平面分离** | 控制平面管理合约/策略/身份，数据平面执行实际计算和数据传输 | Eclipse EDC / Tractus-X |
| **数字清算所模式** | 输出审查网关 + 链上存证形成合规证明链 | GAIA-X Digital Clearing Houses |
| **数据清洁室模式** | 多方参与不共享原始数据，差分隐私 + 密码学计算 + 审计日志 | AWS Clean Rooms |
| **TEE Library OS** | 未修改应用直接在 TEE 中运行，Manifest 配置 + 远程证明 | Gramine / Occlum |
| **数据空间连接器协议** | 标准化连接器间通信协议：合约协商 → 数据传输 → 使用控制 | IDS DSP |

---

## 2. 核心场景设计

### 2.1 场景一：公共数据授权运营

**业务背景**：政府公共数据（政务、交通、医疗、教育等）需要在保障安全的前提下向社会授权运营，释放数据价值。

**参与方**：
- **数据提供方**：政府部门、公共机构
- **数据运营方**：数据交易中心、大数据集团
- **数据使用方**：企业、研究机构
- **安全运维方**：平台运维团队

**业务流程**：

```
① 数据提供方 → 数据产品注册
   - 上传公共数据（CSV/数据库/API）
   - SM4-GCM 加密后存入 MinIO
   - 配置使用策略（允许操作、输出限制、DP参数）
   - 发布至数据目录

② 数据使用方 → 数据产品发现与申请
   - 浏览数据目录，查看质量报告和脱敏预览
   - 选择数据产品，申请使用合约
   - 填写使用目的、预期产出、安全等级要求

③ 数据运营方 → 合约审批
   - 审核使用方资质和使用目的
   - 配置合约条款（期限、操作权限、DP预算）
   - 双方 SM2 数字签名

④ 系统自动 → 沙箱创建与数据加载
   - 合约激活 → KMS 分发 DEK 至 TEE
   - 调度器创建沙箱 Pod（L1/L2/L3 根据安全等级）
   - 连接器将加密数据同步至沙箱内

⑤ 数据使用方 → 沙箱内计算
   - 提交 SQL/Python 代码
   - TEE 内解密数据并执行计算
   - 使用控制引擎实时检查操作合规性

⑥ 系统自动 → 输出审查与结果返回
   - 输出审查网关：DLP扫描 + DP噪声注入 + 水印嵌入
   - SM2 签名 + 链上存证
   - 审查通过的结果返回使用方

⑦ 数据运营方 → 审计与监管
   - 查看全量审计日志
   - 验证区块链存证
   - 导出合规报告
```

**关键安全控制**：
- 数据全程 SM4-GCM 加密（存储 + 传输 + 内存）
- TEE 远程证明确保计算环境可信
- 差分隐私预算按合约控制，防止统计推断攻击
- 所有操作链上存证，不可篡改

### 2.2 场景二：可信数据空间中的沙箱

**业务背景**：可信数据空间（Trusted Data Space）是多个组织间安全共享数据的基础设施。CDS 作为可信数据空间的核心计算节点，提供"数据不动、算法动"的计算模式。

**参与方**：
- **数据空间运营方**：建设并运营可信数据空间
- **数商（数据提供方）**：在空间内发布数据产品
- **数据开发商**：开发数据分析算法和模型
- **数据使用方**：消费数据产品获取分析结果

**业务流程**：

```
① 数据空间运营方 → 空间初始化
   - 部署 CDS 系统
   - 配置 TEE 节点集群（L1 SGX / L2 gVisor / L3 Docker）
   - 建立 SM2 CA 证书体系
   - 配置跨空间互联互通接口

② 数商 → 数据产品开发
   - 通过"开发沙箱"进行数据质量预检
   - 自动生成质量报告（完整率、分布统计、异常值）
   - 配置脱敏规则和使用策略
   - 测试脱敏效果（沙箱内运行测试查询）
   - 发布至数据目录

③ 数据开发商 → 算法开发
   - 获取数据产品 Schema 和质量报告
   - 在沙箱内开发和调试算法
   - 使用特征工程工作台构建特征
   - 在沙箱内训练模型（DP注入 + 防记忆化检查）

④ 数据使用方 → 消费数据产品
   - 浏览数据目录，选择数据产品
   - 签订数字合约
   - 创建沙箱会话，获取 TEE 证明报告
   - 提交计算任务，获取审查后的结果

⑤ 系统自动 → 全程审计
   - 每个操作生成审计日志
   - SM3 Merkle Tree 聚合
   - 批量锚定到 FISCO BCOS 区块链
   - 监管方可随时查询验真
```

### 2.3 场景三：跨域安全流通

**业务背景**：不同数据空间（金融空间、政务空间、医疗空间等）之间需要安全地流通数据产品，实现跨域数据价值融合。

**参与方**：
- **空间 A 运营方**（如金融数据空间）
- **空间 B 运营方**（如政务数据空间）
- **跨域买方**（需要跨空间数据的企业）

**业务流程**：

```
① 空间互认
   - 空间 A 和空间 B 通过互联互通层建立信任
   - SM2 证书互信（跨空间 CA 交叉签名）
   - TLCP 加密通道建立

② 跨空间目录查询
   - 买方（空间B）查询空间A的数据目录
   - 返回产品列表（元数据，不含实际数据）

③ 跨空间合约签订
   - 买方与空间A的数商签订数字合约
   - 合约经双方空间运营方确认
   - 合约锚定到双方区块链

④ 跨空间沙箱会话
   - 买方创建跨空间沙箱会话
   - CDS 验证合约有效性 + 跨空间身份互认
   - TEE 远程证明报告发送给空间A（证明计算环境可信）
   - 空间A的KMS向TEE内分发数据密钥

⑤ 跨空间计算
   - 数据在TEE内解密并执行计算
   - 输出经审查网关（更严格的DP参数）
   - 结果返回买方

⑥ 跨空间审计
   - 审计日志同时写入双方空间
   - 双方区块链各自存证
   - 支持跨空间审计查询
```

---

## 3. 用户角色与完整业务闭环

### 3.1 五类核心用户

| 角色 | 定义 | 核心诉求 | 关键操作 |
|------|------|----------|----------|
| **数据提供商** | 持有数据资产的企业/机构 | 数据不离域、不被复制、使用可控可追溯 | 注册产品、配置策略、查看收益 |
| **数据开发商** | 开发数据分析算法和模型的技术团队 | 算法可自主运行、结果真实可信 | 开发算法、特征工程、模型训练 |
| **数据使用方** | 需要数据做分析/建模的企业 | 获取可信分析结果、成本可控 | 搜索目录、签订合约、提交任务 |
| **运营方** | 数据空间平台建设者 | 合规、可审计、支持互联互通 | 管理节点、监控状态、配置连接器 |
| **安全运维方** | 保障平台安全运行的技术团队 | 零信任运维、安全事件快速响应 | 监控告警、安全审计、应急响应 |

### 3.2 数据提供商完整业务闭环

```
┌─────────────────────────────────────────────────────────────┐
│  数据提供商业务闭环                                            │
│                                                             │
│  ① 机构认证 → 获取SM2证书                                     │
│  ② 数据产品注册                                               │
│     ├── 结构化数据：上传CSV/配置JDBC连接 → Schema自动推断       │
│     ├── 半结构化数据：上传JSON/XML → 结构自动发现               │
│     └── 非结构化数据：上传图像/文档/音视频 → 内容分析           │
│  ③ 开发沙箱（质量预检 + 脱敏预览 + 策略测试）                   │
│     ├── 质量报告自动生成                                       │
│     ├── 敏感字段自动识别                                       │
│     ├── 脱敏效果预览                                           │
│     └── 测试查询验证                                           │
│  ④ 配置使用策略                                               │
│     ├── 允许操作类型（aggregate/filter/join/model/inference）   │
│     ├── 禁止操作（row_level_export/network_call/print）        │
│     ├── 输出约束（maxOutputRows/DP参数/格式限制）              │
│     └── 安全等级（L1/L2/L3）                                   │
│  ⑤ 发布至数据目录                                              │
│  ⑥ 审核买方合约申请（可配置自动审批规则）                        │
│  ⑦ 查看使用监控                                               │
│     ├── 买方访问记录                                           │
│     ├── DP消耗排行                                             │
│     ├── 任务统计                                               │
│     └── 收益结算                                               │
│  ⑧ 数据更新/版本迭代                                           │
│  ⑨ 产品下架/密钥吊销                                           │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 数据使用方完整业务闭环

```
┌─────────────────────────────────────────────────────────────┐
│  数据使用方业务闭环                                            │
│                                                             │
│  ① 机构认证 → 获取SM2证书                                     │
│  ② 数据目录检索                                               │
│     ├── 关键词搜索 + 行业/类型/安全等级筛选                     │
│     ├── 查看产品详情（Schema/质量报告/脱敏预览）                │
│     └── 比较同类产品                                           │
│  ③ 申请合约                                                   │
│     ├── 选择合约类型（查询/训练/API/联合建模）                  │
│     ├── 填写使用目的和预期产出                                  │
│     └── 双方SM2签名                                           │
│  ④ 创建沙箱会话                                               │
│     ├── 选择安全等级                                           │
│     ├── 获取TEE远程证明报告                                    │
│     └── 系统验证合约 → 分发密钥 → 加载数据                     │
│  ⑤ 提交计算任务                                               │
│     ├── SQL查询（结构化数据）                                  │
│     ├── Python脚本（通用计算）                                 │
│     ├── 推理任务（非结构化数据）                                │
│     └── 特征工程 + 模型训练                                    │
│  ⑥ 获取审查后的结果                                           │
│     ├── 表格/图表/文件                                         │
│     ├── 执行报告（耗时/DP消耗/审查结果）                       │
│     ├── SM2签名验证                                           │
│     └── 区块链存证链接                                         │
│  ⑦ 模型部署（三种模式）                                       │
│     ├── 离线导出：模型文件经安全审查后导出                      │
│     ├── API沙箱推理：模型在沙箱内常驻，通过API调用              │
│     └── 联邦推理：多方数据联合计算                              │
│  ⑧ 任务历史追溯                                               │
└─────────────────────────────────────────────────────────────┘
```

### 3.4 运营方完整业务闭环

```
┌─────────────────────────────────────────────────────────────┐
│  运营方业务闭环                                               │
│                                                             │
│  ① 平台部署与初始化                                           │
│     ├── TEE节点集群部署                                       │
│     ├── SM2 CA证书体系建立                                     │
│     ├── KMS + HSM配置                                        │
│     └── 区块链节点部署                                        │
│  ② 运营总览监控                                               │
│     ├── 全局健康状态                                          │
│     ├── 节点状态（SGX/gVisor/容器）                           │
│     ├── 吞吐量（tasks/min）                                  │
│     └── 告警管理                                              │
│  ③ 节点管理                                                   │
│     ├── 添加/移除节点                                         │
│     ├── 安全等级标签                                          │
│     ├── 资源配额管理                                          │
│     └── 熔断器配置                                            │
│  ④ 合约监督（只读）                                           │
│     ├── 查看所有合约状态                                      │
│     ├── 即将到期合约提醒                                      │
│     └── 合约违约告警                                          │
│  ⑤ 审计中心                                                   │
│     ├── 全量审计日志查询                                      │
│     ├── Merkle存证验证                                       │
│     ├── 区块链验真                                            │
│     └── 合规报告导出                                          │
│  ⑥ 连接器配置（跨空间）                                       │
│     ├── 外部数据空间接入                                      │
│     ├── 身份互认配置                                          │
│     ├── TLCP加密通道                                         │
│     └── 跨空间目录同步                                        │
│  ⑦ 用户管理                                                   │
│     ├── 机构注册审批                                          │
│     ├── 角色权限管理                                          │
│     └── 证书管理                                              │
└─────────────────────────────────────────────────────────────┘
```

### 3.5 安全运维方完整业务闭环

```
┌─────────────────────────────────────────────────────────────┐
│  安全运维方业务闭环                                            │
│                                                             │
│  ① 安全基线配置                                               │
│     ├── TEE远程证明策略                                       │
│     ├── Seccomp-BPF系统调用白名单                             │
│     ├── 网络策略（NetworkPolicy）                             │
│     └── 密钥轮转策略                                          │
│  ② 实时安全监控                                               │
│     ├── 策略拒绝率                                            │
│     ├── 异常操作告警                                          │
│     ├── 输出审查拦截统计                                      │
│     └── DP预算消耗率                                          │
│  ③ 安全事件响应                                               │
│     ├── 告警分级（P0-P3）                                    │
│     ├── 自动熔断（异常行为触发）                               │
│     ├── 会话紧急终止                                          │
│     └── 事件溯源分析                                          │
│  ④ 漏洞管理                                                   │
│     ├── TEE固件更新                                          │
│     ├── 依赖项安全扫描                                        │
│     ├── 容器镜像签名                                          │
│     └── 渗透测试                                              │
│  ⑤ 合规审计                                                   │
│     ├── 等保三级检查                                          │
│     ├── 密评（GM/T 0054-2018）                               │
│     ├── TC609互操作认证                                      │
│     └── 审计报告生成                                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 功能模块详细设计

### 4.1 F1：数据产品管理

**功能描述**：数商通过数据产品管理模块，将数据资产封装为沙箱可消费的标准数据产品。

#### 4.1.1 核心功能

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 数据产品注册 | 填写元数据并上传至目录，支持结构化/半结构化/非结构化三种路径 | P0 |
| 密态数据封装 | 原始数据使用SM4-GCM加密后存入MinIO，密钥由KMS托管 | P0 |
| 数据质量预检 | 在TEE内执行脱敏预览，生成质量报告 | P1 |
| 使用策略配置 | 定义允许/禁止操作、输出约束、DP参数 | P0 |
| 版本管理 | 支持数据产品版本迭代 | P2 |
| 产品下架/回收 | 合约到期或主动下架后密钥吊销 | P1 |
| 多表数据产品 | 支持多表定义，买方可执行跨表JOIN | P1 |
| 场景模板 | 数商定义业务场景模板，买方按场景使用 | P1 |

#### 4.1.2 数据产品类型体系

| 类型 | 格式 | 存储 | 查询方式 | 脱敏粒度 |
|------|------|------|----------|----------|
| 结构化 | CSV/Parquet/DB表 | PostgreSQL | SQL | 列级/行级 |
| 半结构化 | JSON/XML/日志 | MinIO | JSONPath/Python | 字段级 |
| 非结构化 | 图像/文档/音视频 | MinIO | 推理/特征提取 | 内容级 |

#### 4.1.3 数据产品元数据模型

```yaml
DataProduct:
  id: "dp-{uuid}"
  name: "产品名称"
  category: "行业分类"
  dataType: ["structured", "semi_structured", "unstructured"]
  securityLevel: "L1"  # L1/L2/L3
  schema:
    tables:
      - name: "表名"
        fields:
          - field: "字段名"
            type: "类型"
            sensitivity: "high/medium/low"
  volumeRows: 5000000
  updateFrequency: "monthly"
  allowedOperations: ["aggregate", "filter", "join", "model"]
  forbiddenOperations: ["row_level_export", "network_call"]
  outputConstraints:
    maxOutputRows: 100
    requireDifferentialPrivacy: true
    epsilon: 1.0
  sandboxConfig:
    maxRuntimeSeconds: 3600
    maxMemoryGB: 16
    allowedLanguages: ["python", "sql"]
  contractRequired: true
```

#### 4.1.4 开发沙箱流程

```
数商选择数据类型
  │
  ├── 结构化数据
  │   ① 上传CSV/配置JDBC连接
  │   ② Schema自动推断（字段名/类型/空值率/唯一值数）
  │   ③ 敏感字段自动识别（正则+值分布+字段名语义）
  │   ④ 质量报告（行数/列数/空值率/分布直方图/异常值）
  │   ⑤ 脱敏预览（高敏列脱敏 + 聚合统计 + 脱敏样本行）
  │   ⑥ 策略配置（允许SQL操作/输出行数/DP参数）
  │   ⑦ 测试查询验证
  │   ⑧ 发布至目录
  │
  ├── 半结构化数据
  │   ① 上传JSON/XML/日志文件
  │   ② 结构自动发现（JSON键路径树/XPath/正则）
  │   ③ 字段级敏感检测（键名+值模式+嵌套深度）
  │   ④ 质量报告（记录数/嵌套深度/字段覆盖率）
  │   ⑤ 脱敏预览（保持JSON结构，替换敏感值）
  │   ⑥ 策略配置 + 测试 + 发布
  │
  └── 非结构化数据
      ① 上传图像/文档/音视频
      ② 格式检测（MIME类型）
      ③ 内容分析（人脸/OCR/ASR/目标检测）
      ④ 质量报告（文件数/大小/格式分布）
      ⑤ 脱敏预览（人脸打码/NER脱敏/说话人匿名）
      ⑥ 策略配置 + 测试 + 发布
```

### 4.2 F2：沙箱运行环境

**功能描述**：密态沙箱的核心计算容器，提供三级硬件隔离的安全执行环境。

#### 4.2.1 沙箱类型

| 类型 | 适用场景 | 计算模式 | 隔离级别 | 启动延迟 | 性能损耗 |
|------|----------|----------|----------|----------|----------|
| **标准分析沙箱** | SQL查询、统计分析 | 单任务独占TEE | L1 (SGX) | ≤60s | ~30% |
| **模型训练沙箱** | ML/DL训练 | 批处理长任务 | L1/L2 | ≤60s | ~20% |
| **联合计算沙箱** | 多数商数据联合分析 | MPC+TEE混合 | L1 | ≤90s | ~40% |
| **API服务沙箱** | 在线推理 | 常驻服务 | L1/L2 | ≤5s | ~10% |
| **多模态分析沙箱** | 图文混合训练 | GPU-TEE | L1 | ≤120s | ~25% |
| **文档理解沙箱** | 合同审查/OCR | 密态OCR+NLP | L2 | ≤15s | ~10% |

#### 4.2.2 三级安全等级

```
┌─────────────────────────────────────────────────────────┐
│  L1  TEE 沙箱（标准级）                                   │
│  • 硬件隔离（Intel SGX / AMD SEV / 国产 TEE）             │
│  • 远程证明（Attestation Report）                         │
│  • 内存全程加密                                           │
│  • 适用：高敏感数据（重要/核心数据分类）                    │
│                                                         │
│  L2  软件增强沙箱（降级级）                                │
│  • Firecracker microVM + gVisor 用户态内核                │
│  • Seccomp-BPF 系统调用过滤                               │
│  • MPC 双方校验 + 更严格DP                                │
│  • 适用：一般敏感数据                                      │
│                                                         │
│  L3  最小化隔离沙箱（兜底级）                              │
│  • Docker + AppArmor/SELinux                             │
│  • 仅允许聚合统计，禁止个体级结果                           │
│  • 适用：低敏/公开数据                                     │
└─────────────────────────────────────────────────────────┘
```

#### 4.2.3 沙箱生命周期

```
[合约激活] → [密钥分发至TEE] → [数据解密入TEE内存]
     ↓
[买方算法/SQL上传并验签] → [TEE内执行] → [输出审查]
     ↓
[结果返回买方] → [任务日志存证] → [TEE销毁/密钥吊销]
```

#### 4.2.4 买方代码验证

```python
# AST白名单检查
ALLOWED_NODES = {
    ast.Module, ast.FunctionDef, ast.Return, ast.Assign, ast.Name, ast.Constant,
    ast.BinOp, ast.UnaryOp, ast.Add, ast.Sub, ast.Mult, ast.Div,
    ast.Compare, ast.BoolOp, ast.If, ast.For, ast.While,
    ast.List, ast.Tuple, ast.Dict, ast.Subscript, ast.Call,
}
BLOCKED_MODULES = {"os", "sys", "subprocess", "shutil", "socket", "http", "urllib"}
BLOCKED_BUILTINS = {"eval", "exec", "compile", "__import__", "globals", "locals", "open"}
```

### 4.3 F3：互联互通接口

**功能描述**：支持本沙箱系统接入多个异构可信数据空间，实现跨空间数据产品消费。

#### 4.3.1 标准接口（对齐TC609 / NDI-TR-2025-02）

| 接口 | 方法 | 描述 |
|------|------|------|
| `/sandbox/register` | POST | 注册沙箱实例，声明能力 |
| `/sandbox/session/create` | POST | 创建沙箱会话，触发密钥分发 |
| `/sandbox/task/submit` | POST | 提交计算任务 |
| `/sandbox/task/result` | GET | 获取经审查的计算结果 |
| `/sandbox/attest` | GET | 获取TEE远程证明报告 |
| `/sandbox/audit/log` | GET | 查询操作审计日志 |
| `/sandbox/contract/bind` | POST | 绑定数字合约 |
| `/crossspace/catalog/query` | GET | 跨空间数据产品目录查询 |
| `/crossspace/identity/verify` | POST | 跨空间身份互认 |

#### 4.3.2 连接器架构

| 连接器类型 | 数据源 | 传输方式 |
|------------|--------|----------|
| internal | 平台内部数据产品 | MinIO直接挂载 |
| api_gateway | 外部API (REST/gRPC) | HTTPS代理 |
| file_connector | 文件系统 (SFTP/NFS) | 加密传输 |
| db_connector | 外部数据库 | CDC流式同步 |
| platform_hook | 第三方数据平台 | Webhook推送 |
| blockchain | 区块链节点 | 链上数据拉取 |

### 4.4 F4：使用控制引擎

**功能描述**：对沙箱内所有数据操作实施实时策略控制。

#### 4.4.1 控制维度

| 维度 | 控制内容 | 执行方式 |
|------|----------|----------|
| 操作控制 | 允许/禁止特定API调用 | Seccomp/TEE系统调用过滤 |
| 时间控制 | 最大运行时长、访问时间窗口 | 任务调度器 + 定时熔断 |
| 数量控制 | 最大查询行数、最大输出量 | 计数器 + 配额管理 |
| 字段控制 | 高敏感字段TEE内自动脱敏 | 字段级策略引擎 |
| 模型控制 | 禁止模型参数/梯度输出 | DP注入 + 梯度裁剪 |
| 身份控制 | 仅合约指定买方可创建会话 | SM2身份验证 |

#### 4.4.2 查询安全控制

```python
# 查询层防脱库引擎
DANGEROUS_PATTERNS = [
    "SELECT *", "INTO OUTFILE", "INTO DUMPFILE", "LOAD_FILE",
    "BENCHMARK(", "SLEEP(", "UNION SELECT", "INFORMATION_SCHEMA",
    "SHOW TABLES", "SHOW DATABASES", "DESCRIBE ", "EXPLAIN ",
]

# 行为模式检测
# - 分批导出：短时间内大量LIMIT offset,count查询
# - 全表扫描：多次查询覆盖所有行
# - 字段枚举：逐个字段查询
# - 异常频率：查询频率突然升高
```

### 4.5 F5：输出审查网关

**功能描述**：沙箱唯一输出通道，对所有离开TEE的结果进行安全审查。

#### 4.5.1 审查流水线

```
TEE计算结果
  → 1. 格式验证（python-magic）
  → 2. DLP扫描（Presidio + HanLP中文NER）
  → 3. DP预算检查（diffprivlib）
  → 4. 噪声注入（Laplace/Gaussian机制）
  → 5. 水印注入（零宽Unicode + LSB）
  → 6. SM2签名
  → 7. 释放
```

#### 4.5.2 安全产出分类

```
✅ 安全产出（可直接出域）
├── 聚合统计：COUNT/SUM/AVG/STDDEV（加DP噪声）
├── 分类标签：行业分类/风险等级/情绪标签
└── 趋势指标：同比/环比/增长率

⚠️ 受控产出（经安全审查后出域）
├── ML模型：训练后的模型文件（需防记忆化检查）
├── 特征向量：聚合后的特征（需确认不可逆）
└── 模型指标：AUC/F1/混淆矩阵（不含样本）

❌ 禁止产出（绝对不出域）
├── 原始数据：任何行级/记录级原始数据
├── 样本数据：训练样本/测试样本
├── 梯度信息：模型训练梯度（可反推数据）
└── 原始文件：图像/文档/音视频原始文件
```

#### 4.5.3 模型导出安全审查

| 检查项 | 描述 | 阈值 |
|--------|------|------|
| 成员推断攻击 | 模型无法判断某条数据是否在训练集中 | AUC < 0.55 |
| 模型反演攻击 | 无法从模型权重重建训练数据 | pass/fail |
| 数据重建测试 | 模型输出不包含原始数据信息 | 相似度 < 0.8 |
| DP审计 | 验证DP噪声是否正确注入 | pass/fail |

### 4.6 F6：存证审计中心

**功能描述**：对沙箱全生命周期操作进行细粒度记录，并通过区块链存证。

#### 4.6.1 审计架构

```
structlog → Fluent Bit → ClickHouse → Merkle Tree → FISCO BCOS
Prometheus + Grafana（指标）
```

#### 4.6.2 审计日志字段

```json
{
  "event_id": "evt-{uuid}",
  "timestamp": "2025-06-01T10:23:45.123Z",
  "sandbox_session_id": "sess-{uuid}",
  "contract_id": "contract-{uuid}",
  "actor": { "space_id": "...", "org_id": "...", "user_id": "...", "role": "..." },
  "action": "task_submit",
  "resource": { "product_id": "...", "data_version": "..." },
  "policy_check": { "result": "allow", "matched_rule": "..." },
  "output": { "row_count": 45, "dp_epsilon": 1.0, "sensitive_fields_redacted": [...] },
  "tee_attestation_hash": "3f2a...b9c1",
  "blockchain_tx": "0xabc...123"
}
```

#### 4.6.3 存证流程

1. 日志生成于TEE内，TEE签名后传输至存证服务
2. 存证服务验签后写入ClickHouse
3. SM3 Merkle Tree 聚合日志哈希
4. 批量锚定到 FISCO BCOS 区块链
5. 监管方可随时查询验真

### 4.7 F7：身份与密钥管理

**功能描述**：基于国密SM2的身份体系，以及与数据产品绑定的密钥生命周期管理。

#### 4.7.1 身份体系

| 实体 | 证书类型 | 签发机构 | 有效期 |
|------|----------|----------|--------|
| 数商机构 | SM2 X.509 | 运营方CA | 1年 |
| 买方机构 | SM2 X.509 | 运营方CA | 1年 |
| 沙箱实例 | TEE平台证书 | 硬件厂商 | 动态 |
| 跨空间互认 | SM2互信证书 | 联盟根CA | 2年 |

#### 4.7.2 密钥管理流程

```
数商上传数据 → SM4密钥加密数据 → 密钥托管至KMS（HSM保护）
                                         ↓
合约激活 → KMS验证合约有效性 → 向TEE内安全分发SM4密钥
                                         ↓
TEE内解密使用 → 合约到期/任务结束 → TEE销毁密钥材料
                                         ↓
数商主动吊销 → KMS吊销密钥 → 所有关联沙箱会话立即终止
```

### 4.8 F8：数字合约集成

**功能描述**：与服务平台数字合约系统对接，以合约为驱动激活/约束沙箱行为。

#### 4.8.1 合约类型

| 类型 | 描述 | 输出 |
|------|------|------|
| 数据查询合约 | 买方提交SQL查询 | 脱敏聚合结果 |
| 模型训练合约 | 买方上传算法训练 | 模型文件（不含原始数据） |
| API服务合约 | 数商部署推理服务 | 预测结果 |
| 联合建模合约 | 多数商数据联合计算 | 模型文件 |

#### 4.8.2 合约驱动映射

| 合约条款 | 沙箱行为 |
|----------|----------|
| 使用期限 | 超期后密钥自动吊销 |
| 允许操作类型 | 映射为使用控制策略包 |
| DP参数ε | 输出审查网关注入对应噪声 |
| 允许访问用户范围 | 身份验证时限定用户列表 |
| 结果输出限制 | 行数/体积上限配置到输出网关 |
| 争议解决条款 | 审计日志自动保留N天供仲裁 |

### 4.9 F9：监控运维中心

**功能描述**：为运营方提供沙箱资源、性能、安全的可观测视图（运维人员不可访问沙箱内数据）。

#### 4.9.1 监控指标

| 类别 | 指标项 |
|------|--------|
| 资源使用 | CPU利用率、内存占用、存储配额消耗 |
| 任务状态 | 队列长度、运行中任务数、失败率、平均执行时长 |
| 安全事件 | 策略拒绝次数、异常操作告警、输出审查拦截次数 |
| 互联互通 | 跨空间会话数、证明报告验证成功率、跨空间延迟 |
| 合约状态 | 活跃合约数、即将到期合约、合约违约告警 |

#### 4.9.2 节点状态面板

```
🟢 sgx-node-1   L1  EPC: 45/64GB  负载: 30%
🟢 sgx-node-2   L1  EPC: 20/64GB  负载: 60%
🟡 gvisor-node-1 L2  MEM: 80/128GB 负载: 75%
🟢 docker-node-1 L3  MEM: 40/64GB  负载: 50%
🔴 offline-node  L1  离线 - 熔断器: OPEN
```

---

## 5. 技术架构与开源选型

### 5.1 系统架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                         接入层 (FastAPI + Kong)                       │
│  数商 SDK (Python)    买方 SDK (Python)    管理控制台 (React)        │
└──────────────────────────────┬──────────────────────────────────────┘
                               │ HTTPS/TLCP (Tongsuo)
┌──────────────────────────────▼──────────────────────────────────────┐
│                    Interconnection Connector (FastAPI)                │
│  Session Mgmt (Redis)    SM2 JWT Auth    Catalog (PostgreSQL)       │
└────────┬─────────────────────┬──────────────────────────────────────┘
         │                     │
┌────────▼──────┐  ┌──────────▼───────────────────────────────────────┐
│ Contract Engine│  │              Data Plane                          │
│ (OPA + FISCO) │  │  Task Scheduler (Celery + Pulsar)                │
│               │  │  Sandbox Runtime (L1/L2/L3)                      │
│               │  │  Output Inspection Gateway                       │
└───────────────┘  └──────────────────────────────────────────────────┘
         │                     │
┌────────▼─────────────────────▼──────────────────────────────────────┐
│                    Audit & Provenance (ClickHouse + FISCO BCOS)       │
└─────────────────────────────────────────────────────────────────────┘
         │
┌────────▼────────────────────────────────────────────────────────────┐
│                    Storage & Key Layer                                │
│  KMS: Vault Transit + Tongsuo (SM2/SM4)    HSM: PKCS#11             │
│  Object Storage: MinIO (SM4-GCM)    DB: PostgreSQL + Redis          │
│  Message Queue: Apache Pulsar                                       │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2 开源组件选型

| 子系统 | 主选组件 | 备选组件 | License |
|--------|----------|----------|---------|
| KMS | HashiCorp Vault Transit | - | MPL-2.0 |
| 国密算法 | Tongsuo (铜锁) | GmSSL | Apache-2.0 |
| PKI/CA | Vault PKI + Tongsuo | cfssl | MPL-2.0 |
| 策略引擎 | OPA (Rego) | Balana (XACML) | Apache-2.0 |
| 区块链 | FISCO BCOS | - | GPL-3.0 |
| 任务队列 | Celery + RabbitMQ | Temporal.io | BSD-3 |
| 消息队列 | Apache Pulsar | Apache Kafka | Apache-2.0 |
| TEE L1 | Occlum | Gramine | BSD-3/LGPL |
| VM L2 | Firecracker | - | Apache-2.0 |
| 容器沙箱 | gVisor (runsc) | - | Apache-2.0 |
| MPC | MP-SPDZ | - | BSD-3 |
| DLP | Microsoft Presidio | - | MIT |
| 差分隐私 | IBM diffprivlib | OpenDP | MIT |
| 水印 | Stegano + 自研Unicode | - | GPL-3.0 |
| 中文NER | HanLP | PaddleNLP | Apache-2.0 |
| 中文OCR | PaddleOCR | - | Apache-2.0 |
| 中文ASR | FunASR (Paraformer) | Whisper | MIT |
| 人脸检测 | InsightFace (SCRFD) | - | MIT |
| 审计日志 | ClickHouse + structlog | - | Apache-2.0 |
| 监控 | Prometheus + Grafana | - | Apache-2.0 |
| API框架 | FastAPI | - | MIT |
| TLCP | Tongsuo | - | Apache-2.0 |
| 跨链 | WeCross | - | Apache-2.0 |

### 5.3 参考项目

| 项目 | 来源 | 参考点 |
|------|------|--------|
| **Eclipse EDC** | Eclipse Foundation | 控制平面/数据平面分离架构 |
| **GAIA-X** | 欧洲数据空间倡议 | 数字清算所、合规引擎、四级合规标签 |
| **SecretFlow** | 蚂蚁集团 | MPC+FL+TEE统一框架 |
| **Gramine** | Intel CCC | SGX Library OS，未修改应用直接在TEE运行 |
| **Occlum** | 蚂蚁集团 | 多进程SGX LibOS |
| **Confidential Containers** | CNCF | K8s TEE集成 |
| **OpenDP** | Harvard/Microsoft | 模块化差分隐私框架 |
| **PySyft** | OpenMined | 联邦学习+DP+安全计算 |
| **IDS** | IDSA | 数据空间连接器协议 |

---

## 6. 安全架构

### 6.1 数据全生命周期安全

```
传输层: SM4-GCM加密传输 (TLCP)
存储层: SM4-GCM加密存储 (MinIO SSE) + DEK由Vault HSM托管
计算层: TEE硬件隔离 + 内存加密
输出层: DLP扫描 + DP注入 + 水印嵌入 + SM2签名
审计层: 结构化日志 + SM3 Merkle Tree + FISCO BCOS存证
```

### 6.2 防脱库安全控制

```
传输层: 数据导入全程SM4-GCM加密传输 (TLCP)
存储层: 磁盘上只有SM4-GCM密文，DEK不可导出
内存层: TEE硬件内存加密 + mlockall禁止swap + 禁止core dump
查询层: SQL解析+AST分析 + 禁止SELECT * + 强制LIMIT + 分批导出检测
代码层: AST白名单 + RestrictedPython + 禁止网络/文件/进程操作
运维层: 运维人员零权限访问沙箱内数据
```

### 6.3 非结构化数据专项安全

| 类型 | 专项保护 |
|------|----------|
| 文档 | 密态OCR + NER脱敏 + 签名区域黑化 + 仅输出结构化抽取 |
| 图像 | 人脸自动打码 + DICOM标签剥离 + 禁止原图输出 |
| 音频 | 声纹脱敏 + 密态ASR + 说话人匿名化 |
| 视频 | 逐帧人脸模糊 + 行为事件标签输出 + 禁止视频片段输出 |

### 6.4 法规合规

| 法规/标准 | 要求 |
|-----------|------|
| 《数据安全法》 | 数据分级分类管理 |
| 《个人信息保护法》 | 个人信息TEE内处理+DP保护 |
| 《网络安全法》 | 等保三级，审计日志≥6个月 |
| TC609-6-2025-01 | 可信数据空间技术架构合规 |
| NDI-TR-2025-02 | 互联互通接口规范 |
| GM/T 密码标准 | SM2/SM3/SM4强制使用 |

---

## 7. 系统集成与部署

### 7.1 Docker Compose（开发环境）

```yaml
version: "3.8"
services:
  postgres:
    image: postgres:15
    environment: { POSTGRES_DB: sandbox, POSTGRES_USER: sandbox }
  redis:
    image: redis:7-alpine
  clickhouse:
    image: clickhouse/clickhouse-server:24-alpine
  rabbitmq:
    image: rabbitmq:3-management
  vault:
    image: hashicorp/vault:1.19
    environment: { VAULT_DEV_ROOT_TOKEN_ID: sandbox-vault-token }
  opa:
    image: openpolicyagent/opa:latest
  api:
    build: .
    command: uvicorn connector.api:app --host 0.0.0.0 --port 8000
  celery-worker-l1:
    build: .
    command: celery -A scheduler.celery_app worker -Q l1_tee --concurrency=4
  celery-worker-l2:
    build: .
    command: celery -A scheduler.celery_app worker -Q l2_gvisor --concurrency=8
  prometheus:
    image: prom/prometheus:latest
  grafana:
    image: grafana/grafana:latest
```

### 7.2 K8s 生产部署

- Namespace: `confidential-sandbox`
- TEE Device Plugin: Intel SGX Plugin DaemonSet
- RuntimeClass: sgx / gvisor / runc
- NetworkPolicy: 每买方独立namespace，沙箱间禁止互访
- ResourceQuota: 按买方组织配额管理
- PodSecurityPolicy: 非root + 最小权限 + Seccomp

### 7.3 性能基准目标

| 操作 | L1 TEE | L2 软件 |
|------|--------|---------|
| 沙箱启动 | ≤60s (P99) | ≤15s (P99) |
| SQL聚合(1亿行) | ≤120s | ≤60s |
| 输出审查延迟 | ≤500ms | ≤200ms |
| 跨空间会话建立 | ≤5s | ≤3s |

---

## 8. 实施路线图

### Phase 1（0-6个月）：核心沙箱能力

- 标准分析沙箱（SGX/国产TEE）
- SQL + Python计算支持
- 基础使用控制（操作、时间、数量维度）
- 输出审查网关（基础规则引擎）
- SM2身份体系 + SM4数据加密
- 数据产品管理（结构化数据）
- 基本前端界面

### Phase 2（6-12个月）：互联互通能力

- TC609标准接口全面实现
- 跨空间身份互认
- 数字合约驱动的沙箱激活
- 区块链存证中心上线
- 差分隐私输出模块
- 连接器框架（API/文件/DB）
- 半结构化数据支持

### Phase 3（12-18个月）：高级沙箱能力

- 联合计算沙箱（MPC + TEE）
- 模型训练沙箱（大内存TEE支持）
- API服务沙箱（在线推理）
- 非结构化数据支持（图像/文档/音视频）
- 多空间联合审计
- 监管接口开放
- 特征工程工作台

### Phase 4（18-24个月）：生态扩展

- 支持≥5类数据空间类型（企业/行业/城市/个人/跨境）
- LLM训练数据管线（预训练语料/SFT/RAG）
- 大模型与沙箱融合
- 自动化合规报告生成
- 数据产品定价与计量结算集成

### 安全认证路线

| 认证 | 标准 | 计划周期 |
|------|------|----------|
| 等保三级 | GB/T 22239-2019 | Phase 1后6个月 |
| 密评 | GM/T 0054-2018 | Phase 1后 |
| TC609互操作 | TC609-6-2025-01 | Phase 2后 |
| DSMM三级 | GB/T 37988-2019 | Phase 3后 |

---

*文档版本：v1.0 | 状态：产品设计完成 | 基于9个spec文件 + 开源项目调研*
